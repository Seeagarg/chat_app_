const baseUrl = 'http://localhost:4002'

const signupApi = `${baseUrl}/user/sign-up`
export  {signupApi}


const loginApi = `${baseUrl}/user/login`
export  {loginApi}

const logOut = `${baseUrl}/user/log-out`
export  {logOut}